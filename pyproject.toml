[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "easylkb"
version = "0.0.0"
description = "Easy Linux Kernel Builder"
requires-python = ">=3.6"

[tool.setuptools]
py-modules = ["easylkb"]

